---
const links = [
  { href: 'https://github.com/lszeray', label: 'GitHub', icon: '&#128187;' },
  { href: 'mailto:josh@example.com', label: 'Email', icon: '&#9993;' },
  { href: '/about#connect', label: 'Connect', icon: '&#128172;' },
  { href: '/research', label: 'Projects', icon: '&#128300;' },
];
---

<div class="card quick-links">
  <h3 class="ql-heading">Quick Links</h3>
  <div class="ql-grid">
    {links.map(link => (
      <a href={link.href} target={link.href.startsWith('http') ? '_blank' : undefined} rel={link.href.startsWith('http') ? 'noopener' : undefined} class="ql-item">
        <span class="ql-icon" set:html={link.icon} />
        <span class="ql-label">{link.label}</span>
      </a>
    ))}
  </div>
</div>

<style>
  .quick-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .ql-heading {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--fg-2);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  .ql-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }
  .ql-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 0.75rem;
    border-radius: 8px;
    background: var(--stat-bg);
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--fg);
    transition: background 0.15s;
  }
  .ql-item:hover {
    background: var(--border);
    color: var(--accent);
  }
  .ql-icon {
    font-size: 1rem;
  }
</style>
