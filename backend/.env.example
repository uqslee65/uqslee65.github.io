# MongoDB URI — use Atlas connection string in production
MONGO_URI=mongodb://localhost:27018/simulator

# GitHub username for /api/repos
GITHUB_USER=lszeray

# Python formulas service URL (local docker-compose only)
FORMULAS_URL=http://formulas:8000

# Allowed CORS origin
CORS_ORIGIN=https://lszeray.github.io

# Server port (Heroku sets this automatically)
PORT=3000

# Umami analytics PostgreSQL password
UMAMI_DB_PASSWORD=change-me-to-a-strong-password
